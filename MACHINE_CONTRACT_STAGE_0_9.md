# Исторический staging-документ machine contract 0.9

Этот файл сохранён только как указатель на завершённый этап P0.1 (#24).

Промежуточная staging-спецификация больше не является актуальным контрактом: `--json --risky`, safe receipt v1 и `partial_success` уже реализованы в итоговом `ssh_relay` 0.9.0.

Источник истины для текущего машинного интерфейса: `MACHINE_CONTRACT.md`.
